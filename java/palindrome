class Solution {
    public boolean isPalindrome(int x) {
        int original=x;
        int r,sum=0;
        while(x>0){
        r=x%10;
        sum=sum*10+r;
        x=x/10;
    }
    if(original==sum){
        return true;
    }else{
        return false;
    }
    }
     
}
